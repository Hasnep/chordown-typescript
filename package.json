{
  "name": "chordown",
  "version": "1.0.0",
  "description": "markdown based chordsheets",
  "main": "dist/src/chordown.js",
  "directories": {
    "test": "tests"
  },
  "dependencies": {
    "fs-extra": "^8.0.1",
    "js-yaml": "^3.13.1",
    "shelljs": "^0.8.3"
  },
  "devDependencies": {
    "@types/chai": "^4.1.7",
    "@types/js-yaml": "^3.12.1",
    "@types/mocha": "^5.2.6",
    "@types/node": "^12.0.2",
    "chai": "^4.2.0",
    "eslint": "^6.8.0",
    "mocha": "^6.1.4",
    "nyc": "^14.1.1",
    "ts-node": "^8.2.0",
    "typescript": "^3.4.5"
  },
  "scripts": {
    "test": "mocha -r ts-node/register tests/**/*.ts",
    "lint": "tslint --config tslint.json --project tsconfig.json -t codeFrame 'src/**/*.ts' 'test/**/*.ts'",
    "fix": "tslint --fix --config tslint.json --project tsconfig.json -t codeFrame 'src/**/*.ts' 'test/**/*.ts'",
    "build": "tsc -p tsconfig.json",
    "watch": "tsc -p tsconfig.json --watch",
    "start": "node dist/src/chordown.js"
  },
  "author": "Hannes Smit",
  "license": "GPL-3.0-or-later",
  "private": true,
  "bin": "dist/src/cli.js"
}
